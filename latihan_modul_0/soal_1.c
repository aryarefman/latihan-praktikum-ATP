#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int A, B, X;
    
    scanf("%d %d", &A, &B);
    X = A + B;
    
    printf("%d + %d = %d", A, B, X);
    
    
    return 0;
}